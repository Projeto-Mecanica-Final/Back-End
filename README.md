# Back-End

O backend está localizado no projeto Back-End-Main e utiliza Java com Spring Boot. Para executá-lo, é necessário possuir o Java 21 instalado O projeto inclui o Maven Wrapper, o que permite a execução sem a necessidade de instalar o Maven manualmente.

Para iniciar o backend, abra um terminal no diretório. Em sistemas Windows, utilize o comando:

mvnw.cmd spring-boot:run

Em sistemas Linux ou macOS, utilize:

./mvnw spring-boot:run

Caso o Maven esteja instalado no sistema e prefira utilizá-lo, o comando é:

mvn spring-boot:run

O backend iniciará normalmente em http://localhost:8084


(BACK END ESTRUTURADO EM OUTROS PROJETOS, UNIFICADO E MANDADO NESSA ORGANIZAÇÃO PARA FAZER A PADRONIZAÇÃO CORRETA DOS CODIGOS: BACK-END'S: Maicon, Pedro,Elessandro)
